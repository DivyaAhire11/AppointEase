CREATE DATABASE doctor_booking;

CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE doctors (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    specialist VARCHAR(100),
    experience VARCHAR(50),
    description TEXT,
    image VARCHAR(255)
);

INSERT INTO doctors (name, specialist, experience, description, image)
VALUES
('Dr. Rahul Sharma', 'Cardiologist', '10 Years',
 'Expert in heart diseases, angioplasty, and cardiac surgery.',
 'doctor1.jpg'),

('Dr. Anjali Mehta', 'Dermatologist', '8 Years',
 'Specialist in skin care, acne treatment, and cosmetic dermatology.',
 'doctor2.jpg');


CREATE TABLE appointments (
    id SERIAL PRIMARY KEY,
    patient_id INT,
    doctor_id INT,
    appointment_date DATE,
    appointment_time TIME,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT unique_booking UNIQUE (doctor_id, appointment_date, appointment_time)
);
